# Этот файл применяется ТОЛЬКО локально (docker compose автоматически его подхватывает).
# На сервер не копировать.
services:
  backend:
    environment:
      DEV_MODE: "true"    # отключает проверку Telegram подписи → можно тестировать через curl
